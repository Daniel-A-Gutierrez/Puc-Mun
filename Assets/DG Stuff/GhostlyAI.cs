using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GhostlyAI : MonoBehaviour
{
    /*
     Lets say we implement the directionality as a graph . The graph tells the ghost which direction to go.
     We could have straight corridors as edges in the graph, and intersections as nodes. 
    */


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
